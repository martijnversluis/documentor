<div class="max-w-lg mx-auto">
  <div class="mb-6">
    <div class="flex items-center space-x-2 text-sm text-gray-500 mb-1">
      <%= link_to "Abonnementen", subscriptions_path, class: "hover:text-gray-700" %>
      <span>/</span>
      <%= link_to @subscription.name, @subscription, class: "hover:text-gray-700" %>
      <span>/</span>
    </div>
    <h1 class="text-2xl font-bold text-gray-900">Abonnement bewerken</h1>
  </div>

  <div class="bg-white rounded-lg shadow-sm border border-gray-200 p-6">
    <%= render "form", subscription: @subscription %>
  </div>
</div>
